import { ComponentConfig, ComponentGroup } from "../../../core/types/editor";

export const TiptapComponentConfig = {
    name: "web-pages:content.tiptap",
    selector_text: "Wysiwyg",
    editor_description: "Flexible text editor",
    group: ComponentGroup.Content,
    has_children: false,
    options: {

    },
} as ComponentConfig;
