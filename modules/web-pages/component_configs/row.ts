import { ComponentConfig, ComponentGroup } from "../../../core/types/editor";

export const GridRowComponentConfig = {
    name: "web-pages:grid.row",
    selector_text: "Row",
    editor_description: "Horizontal alignment of elements",
    group: ComponentGroup.Grid,
    has_children: true,
    options: {},
} as ComponentConfig;
