import { ComponentConfig } from "client/types/editor";

export const ContainerConfig: ComponentConfig = {
    name: "UiPcGridContainer",
    options: {},
    has_children: true,
    selector_text: "Container",
    editor_description:
        "Container component will change its width depending on screen size. Can include other components.",
};
